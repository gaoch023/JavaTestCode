package EJB;

/**
 * 首先定义本地接口ILocalCalculator
 * @author Administrator
 *
 */
public interface ILocalCalculator {
	double add(double x,double y);
	double minus(double x,double y);
	double mutilply(double x,double y);
	double devide(double x,double y);
}
